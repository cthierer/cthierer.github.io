/**
 * @flow
 */

import { Responsive } from 'semantic-ui-react'

export default Object.freeze({
  xs: Responsive.onlyMobile,
  sm: Responsive.onlyTablet,
  md: Responsive.onlyComputer,
  lg: Responsive.onlyLargeScreen,
  xl: Responsive.onlyWidescreen,
})
